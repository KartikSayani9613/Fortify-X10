import x10.io.console;
import x10.lang.Math;
import x10.array.Array_1;
import x10.array.Array_2;
import x10.array.Array_3;
/*needs to import
*/
/*exports
export Executable
*/

public class fact64{
	 static var n:Long = 20l;
	private static k:String = "myboi";
	 static val i: Int = 0n; static val j: Int = 0n;
	 static val i = 0.0f; static val j = "yolo";
	public static def main(args:Rail[String]){	
	for(i in n)something
	}
	}
	}
	 static def f(x:Long):Long{	
	if(x) One
	}
	else 
		x
		}
	}
	}
}
